#include<iostream>
#include<vector>
using namespace std;

int main()
{
    vector<int>v;
    
     cout<<v.capacity()<<endl;
    v.push_back(10);
     v.push_back(20);
      v.push_back(30);
       v.push_back(40);
        v.push_back(50);
         v.push_back(60);
          v.push_back(70);
          
          //cout<<v[0]<<endl;
          //v[7]=80;
          //cout<<v.size()<<endl;
          //cout<<v[7]<<endl;
    
   /* v.pop_back();
    v.pop_back();
    v.pop_back();
    v.pop_back();
    v.pop_back();
    v.pop_back();
    v[0]=100000;
    */
    cout<<v.capacity()<<endl;
    
    for(int i=0;i<v.size();i++)
    cout<<v.at(i)<<endl;
    
    
    
    
}
